---
alwaysApply: false
description: 羚羊工业 Tauri 客户端开发规范，涵盖核心库、项目结构、配置与日志分层
globs:
  - "**/src-tauri/**/*"
  - "**/tauri.conf.json"
  - "**/config.toml"
---

# Tauri 客户端开发规范

> 综合集团统一规范 + 羚羊规范，适用于羚羊工业 Tauri 桌面/客户端项目。标注【强制】必须遵守，【推荐】建议遵守。

## 一、核心库与项目结构

### 1.1 核心库

- 【强制】所有 Tauri 客户端项目**必须使用**统一的 `packages/tauri-core`（或项目约定的核心库）
- 【强制】入口通过核心库启动，禁止在客户端重复实现核心库已提供的功能

```rust
// src-tauri/src/lib.rs
use tauri_core;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri_core::run_app(tauri::Builder::default())
}
```

### 1.2 客户端项目结构

- 【强制】客户端 `src-tauri/` 内只保留：`src/lib.rs`、`src/main.rs`、`icons/`、`config.toml`、`tauri.conf.json`、`Cargo.toml`、`keys/`（如需签名）
- 【强制】**禁止在客户端重复实现**：音频采集/播放、ASR/TTS、网络监控、自动更新、日志管理、配置加载等；一律使用核心库能力

### 1.3 新增能力

- 【强制】新增 Tauri Command、系统能力等应在**核心库**中实现并注册，各客户端通过核心库调用

## 二、配置管理

### 2.1 端口与前端

- 【推荐】开发端口统一在 `.env.development`（如 `VITE_PORT=5174`），通过脚本同步到 `tauri.conf.json`，避免手改

### 2.2 业务配置

- 【推荐】项目级配置放在 `src-tauri/config.toml`（如唤醒词、业务开关），修改后需重启应用生效
- 【推荐】配置加载日志使用 **debug** 级别，避免每次调用都打 info

### 2.3 编译输出

- 【推荐】使用 Cargo workspace 时，编译输出统一到根目录（如 `tauri-target/`），在 `.cargo/config.toml` 中配置 `target-dir`，便于共享缓存与磁盘管理

## 三、日志分层

- 【强制】**严格区分**网络日志与业务日志，写入不同文件（如 `network.log` 与 `main.log`）

### 3.1 network.log（网络相关）

- 【强制】以下内容**只**写入 network 专用接口（如 `write_network_log()`），不写入 main.log：
  - WebSocket：连接成功/失败/关闭、通道关闭
  - HTTP：请求发送、成功/失败、响应接收
  - ASR/TTS 协议：握手、音频发送、转写结果、协议解析错误
  - 网络错误：超时、DNS 失败、连接失败、发送失败
  - 网络监控：启停、状态变化

### 3.2 main.log（业务逻辑）

- 【强制】以下内容使用 **tracing**（`tracing::info!` / `warn!` / `error!` / `debug!`）写入 main 日志：
  - 服务生命周期：启动/停止、初始化完成
  - 配置加载：成功/失败（建议 debug）
  - 业务状态、状态机、音频处理、唤醒词、指令、播放控制等

### 3.3 原则

- 【强制】同一条日志**不要**既打 tracing 又写 network_log，只选一种
- 【推荐】配置加载等频繁、非关键信息用 **debug** 级别

## 四、Rust 侧约定（与 Rust 规范一致）

- 【强制】使用 trait 的方法前**必须导入对应 trait**（如 `use tauri::Emitter` 再调用 `emit()`）
- 【强制】导入顺序：标准库 → 第三方 → 本地 crate
- 【推荐】错误信息使用中文；使用 `Result<T, E>` 与 `?` 传播错误
- 【推荐】使用 `rustfmt` 格式化；公共 API 添加文档注释，关键逻辑补充单元测试

## 五、开发与构建流程

- 【推荐】新建客户端：复制现有项目结构 → 改 name/identifier/config → 清理密钥与前端业务代码 → 生成新密钥 → 安装依赖并跑通 `pnpm dev`
- 【推荐】前端通过 `@tauri-apps/api` 调用后端；新能力优先在核心库实现再暴露给各端

## 六、常见问题

- **emit 报错**：未导入 `use tauri::Emitter`
- **重复实现**：任何与核心库重叠的功能都应删掉，改为调用核心库
- **日志混用**：网络相关只用 `write_network_log`，业务只用 tracing，不重复写入
